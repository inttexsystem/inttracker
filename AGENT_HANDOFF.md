# Agent Handoff
<!-- GENERATED_COMPATIBILITY_VIEW: docs/governance/current-state.json via scripts/governance/render-unit4-canonical-views.mjs; NO INDEPENDENT AUTHORITY -->

This generated continuation view owns no rules, state, product semantics, or acceptance.

## Routing

- Repository: `ravatexapps-dotcom/controle-tapetes-staging`
- Workspace: `D:\Programação\controle-tapetes-g28`
- Branch: `dev`
- Publication boundary: `staging/dev`; EXPLICIT_SINGLE_FAST_FORWARD_ONLY

## Current objective

- Status: `IMPLEMENTED / UIC-006 = 0 / EVERY BLOCKING CONFORMANCE RULE CLOSED / DETECTOR 1.0.6 UNCHANGED / TOTAL 886 -> 865 / COVERAGE 549 -> 543 / 44 SITES = 43 INTERACTIVE POPOVERS + 1 READ-ONLY / ALL 43 ACCESSIBLY NAMED / LOCALLY AND RUNTIME VERIFIED / A5 REMOVED THE DUPLICATE LEGACY CHEVRONS AT THE THREE OP-NOVA SITES / PUBLISHED / AWAITING ARCHITECT ACCEPTANCE`
- Objective: Pass 7 closed UIC-006, the LAST blocking conformance rule. The real population was 44 single-choice sites, not the detector-visible 15: 15 native <select> constructions in js/screens/ plus 29 call sites of the shared js/ui.js::selectInput(), which is the implementation OWNER and is counted apart. ONE OWNER. js/select-popover.js exports window.createSelectPopover(); selectInput() is a thin adapter. 43 sites are the canonical popover; exactly one — Pedido "Status inicial", a single permanently disabled option — became a static read-only field. Two hidden opacity:0 native selects behind styled facades are gone. The primitive accepts an injected document/window, so both injected-document modals drive the same owner. No native <select>, no native <option>, no hidden value owner survives on any product surface. DETECTOR. UIC-006 15 -> 0. Six UIC-000 gaps died with the selects (each was an unresolved style expression ON a native select or its facade), so total 886 -> 865 and coverage 549 -> 543. Detector 1.0.6 byte-identical; nothing suppressed. FOUR ROUNDS. R1 hard-stopped with zero writes on accepted pass-3 guards that required js/ui.js to build a native select. A1 implemented and hard-stopped before commit when frozen cross-pass inventories moved. A2 applied the authorized forward corrections to passes 2, 3, 4, 5 and 6 — all of which remain CLOSED and ACCEPTED; only stale evidence guards moved. A3 hard-stopped with zero writes on nineteen unnamed comboboxes. A4 corrected exactly two product defects. A4. role="combobox" does not take its accessible name from content, unlike a plain button; five screens rendered the visible label as a SIBLING <label>. Nineteen triggers are now bound through aria-labelledby at their owning field helper with a deterministic per-module id sequence, never overwriting an explicit name. One row-level control is a grid cell with no individual label and is named by its own column header. Separately op-nova styleSelect() was overwriting the trigger geometry at three sites (41px/14px/9px/inline-block); it now returns a canonical trigger untouched. LESSON. A1 and A2 measured the primitive directly and reported every site clean. Both A4 defects live in the SCREENS OWN WRAPPERS and are invisible unless the screen helper runs and the accessibility tree is read. CROSS-PASS COUNTERS. RATIFIED_FIELD_FOCUS_RING_COUNT 3 -> 4; CARD_SHAPED_CONSTRUCTION_COUNT 133 -> 125 (eight select facades); ACTION_CONSTRUCTION_COUNT 485 and ACTION_BEARING_FILE_COUNT 39 unmoved; first-party runtime assets 102 -> 103 (js/select-popover.js is the only addition). DISCLOSED. The in-app browser accessibility snapshot does not resolve aria-labelledby — proven with a plain <input aria-labelledby> control case — so naming evidence is DOM-level reference integrity plus target text. Authenticated routes were unreachable, so per-site evidence is computed DOM rather than per-route screenshots. DEVIATION REQUIRING RATIFICATION. The A4 order named the row-Modelo naming source as the "MODELO / CORES" column header; that header belongs to a different read-only table with zero comboboxes. The table that renders the control is headed "MODELO", and that real header was used. A5. Direct architect review of the published checkpoint found that op-nova wrapSelect() still appended the legacy selectChevron() over the canonical trigger, which renders its own 14px chevron — a visible duplicate at Cliente, the row Modelo cell and Fornecedor de tecelagem. The wrapper existed only to anchor that absolutely-positioned chevron, so wrapSelect() now returns a canonical trigger unchanged and keeps the legacy path for a real native control. js/screens/op-nova.js is the only product file changed.
- Next authorizable action: `ARCHITECT ACCEPTANCE OF UI-CONSOLIDATION-PHASE-5-PASS-7 AS CORRECTED BY A2 AND A4 - THE CANONICAL SELECT POPOVER, THE 44-SITE POPULATION, THE CROSS-PASS FORWARD CORRECTIONS AND THE ACCESSIBLE-NAME CONTRACT` / `ARCHITECT TECHNICAL AND VISUAL ACCEPTANCE OF THE NATIVE-SELECT PROPERTY, PLUS RATIFICATION OF THE ROW-MODELO COLUMN-HEADER NAMING DEVIATION`

## Blockers and decisions

- Blockers/debts: `INGESTOR-DOC-CYCLE-VERIFY-DEFERRED`, `CAMADA3-BK5-BK8`, `DELETE-PROD-GUARD-A`, `CODE-HEALTH-AUDIT-18-R1`, `HISTORICAL_SALDO_FIOS_PROVENANCE_UNAVAILABLE`, `NATIVE_RECEIPT_COMPATIBILITY_MULTI_ORIGIN_UNRESOLVED`, `C3D-13-UNMAPPED-ROWS-COMPLETENESS-GATE`, `G14-C-BRIDGE-EXTERNAL-CORPUS-DEPENDENCY`, `DEBT-1-ATRIBUIR-FORNECEDOR-FIO-SEM-CHAMADOR`, `DUPLICATE-OVERLAPPING-EXECUTOR-DISPATCH`, `UI-SMOKE-TEST-VISUAL-EXPECTATION-COUPLING`, `DOCUMENTS-CATALOG-METADATA-DRIFT`, `PROTECTED-RESIDUE-CONTENT-DISCLOSURE`, `UI-SPECIALIZED-CONTROL-CONTRACT-GAP`, `UI-KNOCKOUT-HALO-CONTRACT-GAP`, `UI-ACTION-CONTAINER-CONTAINMENT-GAP`
- Open architect decisions: Accept or reject the CORRECTED UI-CONSOLIDATION-PHASE-4-DETERMINISTIC-CONFORMANCE-DETECTOR-R1: the three-state UIC-001 context classification, the level-two archetype-block boundary, the regenerated baseline at blob 4fb639f035fdc7fa5b344aefad3c1ac473389121, and the two documentary corrections. The executor does not self-accept. | Confirm the closed key and attribute sets that decide UIC-001 proven-visual versus proven-non-visual classification. They are the whole basis of the rule's severity, they are deliberately conservative - an ambiguous key such as bg, text or value is left unclassified and becomes a coverage gap rather than a defect or an absolution - and widening or narrowing either set moves findings between blocking and coverage. | Decide how the 771 UIC-001 coverage gaps are resolved in phase 5. They are colour-shaped runs whose site the detector cannot prove: local variables, ambiguous map keys, array elements and call arguments. Reaching zero on --rule UIC-001 --enforce would not clear them, so pass 1 is not complete until they are classified by moving each value into a declaration, a token or a clearly named key. | Decide how the 25 external prototype screens reach this repository. Exactly one of the 26 archetype rows in UI_CONFORMANCE.md resolves to a tracked file; the other 25 live in the external design project, are reported UNRESOLVED, and cannot be audited or remediated until they are versioned here. | Decide whether the 42 ACTION_ROW_UNPROVEN coverage gaps are closed by marking data-card-actions rows before pass 6 is opened. UIC-008 shows 0 blocking and 42 gaps; declaring the pass clean while 42 screens cannot be evaluated for it would be exactly the false pass the structure exists to prevent. The population grew 37 -> 42 in A2, which added no marker: five screens that had been FULL by absence began declaring canonical geometry and so became evaluable for the first time. A future alignment order must reconcile the WHOLE 42-gap corpus before marking any row; correcting only the five newly visible files would close the symptom and leave the population unproven. | Authorize or defer UI consolidation phase 5, the property-based batch remediation. It is NOT authorized by the phase-4 checkpoint. Its precondition - a detector with a deterministic repository-wide baseline - is now satisfied. | Decide how Archetype A reaches ratification. Phase 4 satisfied one of the two missing preconditions - the detector now exists and has run - but archetype A still has exactly ONE detector-passing screen and its candidate second screen, OP Acabamento - Aberta.dc.html, is not in this worktree. No archetype is RATIFICATION_ELIGIBLE and none may be cited as precedent. | Give or withhold explicit VISUAL acceptance of the BATCH-03 compact layout on #/pedidos/novo; geometry was measured but no screenshot exists, and the executor does not self-accept visual behavior. | Await Kleber's operational review of the application on shared development and then group the reported concrete defects into ONE bounded stabilization order; do not correct them through isolated microphases. | Decide when and under what order the Manta direct-route backend is promoted beyond shared development; production and Vercel remain prohibited without a new explicit order. | Rule on historical saldo_fios provenance before any real receipt cutover. | Resolve native receipt compatibility for multiple purchase-order origins. | Decide whether to retire window.atribuirFornecedorFioOp or restore a legitimate owner/caller. | Decide how the documents-ingestor bridge corpus dependency is satisfied for tests/g14-c-bridge-smoke.test.js. | Order a separate reviewed correction for the docs/governance/catalog/documents.json metadata drift.

## Prohibitions

- NO FURTHER PRODUCT IMPLEMENTATION WITHOUT A NEW ORDER | NO FURTHER DATABASE, SUPABASE, SQL, MIGRATION, ACL/RLS, AUTH, OR ENVIRONMENT ACCESS | NO DEPLOYMENT, ACTIVATION, CUTOVER, PONR, OR PRODUCTION ACTION | NO MAIN, ORIGIN, TAG, ADDITIONAL REF, FORCE, OR SECOND PUBLICATION ATTEMPT | NO CLEANUP, ARCHIVAL, DEPRECATION, PARTITIONING, OR PHYSICAL DELETION | NO MANUAL EDIT OF GENERATED HANDOFF OR OTHER GENERATED OUTPUT OUTSIDE ITS RENDERER | NO ACCESS TO PROTECTED RESIDUE EXCEPT EXPLICIT NON-DISCLOSING HASH EQUALITY | NO AUTOMATIC PRODUCT OR GOVERNANCE PHASE CHAINING | NO PHASE-5 SCREEN REMEDIATION WITHOUT A NEW EXPLICIT ORDER; PHASE 4 IS IMPLEMENTED AND THE DETECTOR MAY BE RUN READ-ONLY, BUT NO FINDING MAY BE REMEDIATED | NO WAIVER, IGNORE-LIST OR SUPPRESSION MECHANISM IN THE CONFORMANCE DETECTOR WITHOUT A NEW EXPLICIT ORDER | NO SECOND SCREEN CONFORMANCE AND NO ARCHETYPE RATIFICATION FROM ONE CONFORMING SCREEN; NO ARCHETYPE IS RATIFICATION_ELIGIBLE | NO ISOLATED MICROPHASE CORRECTION OF KLEBER REVIEW DEFECTS; THEY MUST BE GROUPED INTO ONE BOUNDED STABILIZATION ORDER | NO REAL BUSINESS-FLOW RECREATION WITHOUT A NEW EXPLICIT ORDER | SUPERSEDED UNIT 5A DIRECTION IS CANCELLED

## Task-specific pointers

- `docs/ledgers/G28_LEDGER.md::## 2026-07-27 — UI-CONSOLIDATION-PHASE-5-PASS-7-NATIVE-SELECT-R1 (A1 · A2 · A3 · A4 · A5) — fix(ui): replace native selects with owned popovers`
- `docs/architecture/ORDEM_COMPRA_LIFECYCLE_SPEC_PROPOSED.md::## §R.31 Active Phase-C continuation requirement registry — governance metadata`
- `docs/architecture/PEDIDO_OP_SCHEMA_CONTRACT.md::### 13.17 Active Phase-C schema requirement registry — governance metadata`
- `docs/architecture/PEDIDO_PRODUCTION_FLOW_BACKLOG.md::# Update 2026-07-22 - C5-DOCUMENTATION-CLOSEOUT-R1 (PHASE-C5 supervisor acceptance and closeout; OC-C5-EMISSION-001 SATISFIED)`
- `docs/governance/traceability/purchase-order-phase-c.json::/requirements`
