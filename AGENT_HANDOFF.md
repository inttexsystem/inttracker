# Agent Handoff
<!-- GENERATED_COMPATIBILITY_VIEW: docs/governance/current-state.json via scripts/governance/render-unit4-canonical-views.mjs; NO INDEPENDENT AUTHORITY -->

This generated continuation view owns no rules, state, product semantics, or acceptance.

## Routing

- Repository: `ravatexapps-dotcom/controle-tapetes-staging`
- Workspace: `D:\Programação\controle-tapetes-g28`
- Branch: `dev`
- Publication boundary: `staging/dev`; EXPLICIT_SINGLE_FAST_FORWARD_ONLY

## Current objective

- Status: `IMPLEMENTED / GENERIC CONTROL HEIGHT CLOSED ACROSS SCREENS AND SHARED RUNTIME / UIC-003 0 BLOCKING 0 COVERAGE / DETECTOR 1.0.4 / EXACT 22-0 DELTA / LOCALLY AND RUNTIME VERIFIED AT DEVICE SCALE 1 / NO SCREENSHOT EVIDENCE / PUBLISHED / AWAITING ARCHITECT ACCEPTANCE`
- Objective: Pass 3 closed the GENERIC control-height property and nothing else. RESULT. (1) UIC-003 is 0 blocking / 0 coverage. The measured delta against published checkpoint 7577f93 is EXACTLY 22 findings removed and 0 added, all UIC-003; no other rule moved at all - UIC-000 stays 549 with zero removals, UIC-004 3/8, UIC-005 80, UIC-006 15, UIC-008 0 blocking / 42 coverage, UIC-009 322 debt, UIC-001/002/007/010/011 all zero, FULL 23 / PARTIAL 44 / UNSUPPORTED 0, inventory 66. Baseline sha-256 a4d48f84f2dbfee7891a62ff13c360ae2f31d517bdd3ead9733a39e257b50e74, regenerated twice byte-identically. (2) THE PASS DID NOT RATIFY SPECIALIZED CONTROL GEOMETRY. The first R1 attempt hard-stopped with zero writes because reaching 0/0 by remediation would have forced checkboxes, switch tracks and multiline textareas onto a ladder the contract never defined for them. The A1 order resolved it architecturally: input[type=checkbox|radio|range|hidden], every textarea and every statically proven visually hidden control are EXCLUDED from the generic ladder, and their real geometry is carried by the new open debt UI-SPECIALIZED-CONTROL-CONTRACT-GAP - 21 sites frozen by semantic signature, with a guard that fails both if a site disappears and if a new specialized site appears anywhere in the loaded runtime. Excluded is not conforming. (3) Detector 1.0.4 adds one bounded UIC-003-local predicate, isGenericControlHeightTarget(), which answers "statically specialized or visually hidden" BEFORE role resolution and then falls through to unchanged isControl() semantics. isControl() is untouched, so UIC-006, UIC-007 and UIC-010 cannot observe the change. The visually hidden proof is structural and complete-or-nothing. No path, line, value or filename exception, and no waiver, ignore-list or suppression mechanism. (4) TWO js-screen FRONT-END TRANSPORT GAPS WERE CLOSED, both already present in the dc-html front-end: the literal input type was never transported into attrMap, and static factory bindings did not survive the window. prefix the product uses everywhere. Both are narrow - a literal input tag, a bare literal type key and a quoted static value; a single leading window. before an already-known factory with a literal tag argument. A dynamic type or tag stays UNPROVEN and is never guessed, and neither amendment transports role, data-ui-control, ancestry or interaction semantics. The window. amendment was measured to move exactly one finding and nothing else. (5) Treatment: modal save 38px and cancel 34px in cadastros.js and admin-usuarios-modal.js; copy-password 34px; the documentos-recebidos scan play/pause 32px with width 20px preserved; both cadastros price-row actions delegate to the shared 30x30 actionButton with confirmExcluir still gating deletion; the three pagination current-page nodes become span[aria-current=page] non-interactive indicators keeping exact 30x30 geometry, pixel-aligned with their two canonical neighbours; the two switch tracks declare 40x22 at construction so paint() only repaints; the hidden toggle checkboxes keep their box at construction; corPreviewElement gained an optional size defaulting to the original 48px so three colour swatches stop mutating .style.height. (6) js/ui.js IS NOW THE CANONICAL OWNER OF SINGLE-LINE FIELD HEIGHT. textInput and selectInput declare height:var(--rv-h-compact) and drop py-2, which would clip a fixed border-box height; horizontal px-3, value, required, step, option construction and selection coercion are unchanged. A pre-edit freeze proved neither primitive can build a specialized control - textInput is called only with date, email, number, password, text and the untyped default, and selectInput hard-codes a literal select tag. This intentionally reaches every consumer of the shared primitives across 17 files; the movement from roughly 44px to 32px is the required contract correction, not specialized-control debt, and THE EXECUTOR DOES NOT SELF-ACCEPT IT. (7) The generic mixed-type appliers now branch by real tag: applyCadastrosModalControlStyle and applyAdminUsuariosControlStyle drop the competing 44px minimum for INPUT and SELECT and reconcile only vertical padding, preserving horizontal 13px and the select arrow gutter, while each keeps a separate TEXTAREA branch; MOVEMENT_CONTROL_HEIGHT 36px is split into MOVEMENT_TEXTAREA_MIN_HEIGHT applied only to textareas. No generic control-height constant holds a literal anywhere in the runtime. (8) The first-party runtime guard reports OUT_OF_LADDER_GENERIC_CONTROL_HEIGHT_COUNT 0, TAILWIND_GENERIC_CONTROL_HEIGHT_UTILITY_COUNT 0, UNRESOLVED_GENERIC_CONTROL_ROLE_COUNT 0 and GENERIC_CONTROL_HEIGHT_LITERAL_CONSTANT_COUNT 0 over every local script index.html loads. Height utilities on proven non-controls are listed and justified, never silently skipped. (9) Runtime proof at 1440x900 deviceScaleFactor 1, cold and warm: every textInput type and selectInput variant exactly 32px with horizontal padding unchanged at 12px; modal-helper fields 32px with 13px/38px padding preserved and the textarea branch at its own minimum; primary 38px at the longest real label, secondary 34px, compact 32px, table-row action 30x30, pagination indicator 30x30 with all three row items sharing an identical top; switch track 40x22, knob 18x18, hidden checkbox 0x0 still the state owner, textarea autosize and minimums intact, sr-only logout still a callable 1x1 button. Zero console errors, zero page errors, zero failed local requests, zero unresolved custom properties, zero horizontal overflow, zero clipping, no copy or behaviour change. (10) DISCLOSED GAP - NO SCREENSHOT EVIDENCE EXISTS. Screenshot capture requires a composited browser pane, which this environment did not provide. All runtime evidence is numeric geometry read from the live DOM. No screenshot is claimed. (11) Full sweep has ZERO REGRESSIONS: every remaining failure also fails at a read-only clone of 7577f93, in exactly nine files - tests/g14-c-bridge-smoke.test.js plus the eight previously disclosed .mjs harnesses. None was repaired under this order. (12) NO PASS-4 WORK IS AUTHORIZED. The 42 UIC-008 coverage gaps remain open and unmarked. Shadow, typography, native-select replacement and deprecated-alias deletion remain unauthorized.
- Next authorizable action: `ARCHITECT ACCEPTANCE OF UI-CONSOLIDATION-PHASE-5-PASS-3 (GENERIC CONTROL HEIGHT), ITS DETECTOR 1.0.4 CORRECTION, ITS TWO FRONT-END TRANSPORT AMENDMENTS AND THE OPEN UI-SPECIALIZED-CONTROL-CONTRACT-GAP` / `ARCHITECT TECHNICAL AND VISUAL ACCEPTANCE OF THE GENERIC CONTROL-HEIGHT PROPERTY CLOSED ACROSS SCREENS AND SHARED RUNTIME`

## Blockers and decisions

- Blockers/debts: `INGESTOR-DOC-CYCLE-VERIFY-DEFERRED`, `CAMADA3-BK5-BK8`, `DELETE-PROD-GUARD-A`, `CODE-HEALTH-AUDIT-18-R1`, `HISTORICAL_SALDO_FIOS_PROVENANCE_UNAVAILABLE`, `NATIVE_RECEIPT_COMPATIBILITY_MULTI_ORIGIN_UNRESOLVED`, `C3D-13-UNMAPPED-ROWS-COMPLETENESS-GATE`, `G14-C-BRIDGE-EXTERNAL-CORPUS-DEPENDENCY`, `DEBT-1-ATRIBUIR-FORNECEDOR-FIO-SEM-CHAMADOR`, `DUPLICATE-OVERLAPPING-EXECUTOR-DISPATCH`, `UI-SMOKE-TEST-VISUAL-EXPECTATION-COUPLING`, `DOCUMENTS-CATALOG-METADATA-DRIFT`, `PROTECTED-RESIDUE-CONTENT-DISCLOSURE`, `UI-SPECIALIZED-CONTROL-CONTRACT-GAP`
- Open architect decisions: Accept or reject the CORRECTED UI-CONSOLIDATION-PHASE-4-DETERMINISTIC-CONFORMANCE-DETECTOR-R1: the three-state UIC-001 context classification, the level-two archetype-block boundary, the regenerated baseline at blob 4fb639f035fdc7fa5b344aefad3c1ac473389121, and the two documentary corrections. The executor does not self-accept. | Confirm the closed key and attribute sets that decide UIC-001 proven-visual versus proven-non-visual classification. They are the whole basis of the rule's severity, they are deliberately conservative - an ambiguous key such as bg, text or value is left unclassified and becomes a coverage gap rather than a defect or an absolution - and widening or narrowing either set moves findings between blocking and coverage. | Decide how the 771 UIC-001 coverage gaps are resolved in phase 5. They are colour-shaped runs whose site the detector cannot prove: local variables, ambiguous map keys, array elements and call arguments. Reaching zero on --rule UIC-001 --enforce would not clear them, so pass 1 is not complete until they are classified by moving each value into a declaration, a token or a clearly named key. | Decide how the 25 external prototype screens reach this repository. Exactly one of the 26 archetype rows in UI_CONFORMANCE.md resolves to a tracked file; the other 25 live in the external design project, are reported UNRESOLVED, and cannot be audited or remediated until they are versioned here. | Decide whether the 42 ACTION_ROW_UNPROVEN coverage gaps are closed by marking data-card-actions rows before pass 6 is opened. UIC-008 shows 0 blocking and 42 gaps; declaring the pass clean while 42 screens cannot be evaluated for it would be exactly the false pass the structure exists to prevent. The population grew 37 -> 42 in A2, which added no marker: five screens that had been FULL by absence began declaring canonical geometry and so became evaluable for the first time. A future alignment order must reconcile the WHOLE 42-gap corpus before marking any row; correcting only the five newly visible files would close the symptom and leave the population unproven. | Authorize or defer UI consolidation phase 5, the property-based batch remediation. It is NOT authorized by the phase-4 checkpoint. Its precondition - a detector with a deterministic repository-wide baseline - is now satisfied. | Decide how Archetype A reaches ratification. Phase 4 satisfied one of the two missing preconditions - the detector now exists and has run - but archetype A still has exactly ONE detector-passing screen and its candidate second screen, OP Acabamento - Aberta.dc.html, is not in this worktree. No archetype is RATIFICATION_ELIGIBLE and none may be cited as precedent. | Give or withhold explicit VISUAL acceptance of the BATCH-03 compact layout on #/pedidos/novo; geometry was measured but no screenshot exists, and the executor does not self-accept visual behavior. | Await Kleber's operational review of the application on shared development and then group the reported concrete defects into ONE bounded stabilization order; do not correct them through isolated microphases. | Decide when and under what order the Manta direct-route backend is promoted beyond shared development; production and Vercel remain prohibited without a new explicit order. | Rule on historical saldo_fios provenance before any real receipt cutover. | Resolve native receipt compatibility for multiple purchase-order origins. | Decide whether to retire window.atribuirFornecedorFioOp or restore a legitimate owner/caller. | Decide how the documents-ingestor bridge corpus dependency is satisfied for tests/g14-c-bridge-smoke.test.js. | Order a separate reviewed correction for the docs/governance/catalog/documents.json metadata drift.

## Prohibitions

- NO FURTHER PRODUCT IMPLEMENTATION WITHOUT A NEW ORDER | NO FURTHER DATABASE, SUPABASE, SQL, MIGRATION, ACL/RLS, AUTH, OR ENVIRONMENT ACCESS | NO DEPLOYMENT, ACTIVATION, CUTOVER, PONR, OR PRODUCTION ACTION | NO MAIN, ORIGIN, TAG, ADDITIONAL REF, FORCE, OR SECOND PUBLICATION ATTEMPT | NO CLEANUP, ARCHIVAL, DEPRECATION, PARTITIONING, OR PHYSICAL DELETION | NO MANUAL EDIT OF GENERATED HANDOFF OR OTHER GENERATED OUTPUT OUTSIDE ITS RENDERER | NO ACCESS TO PROTECTED RESIDUE EXCEPT EXPLICIT NON-DISCLOSING HASH EQUALITY | NO AUTOMATIC PRODUCT OR GOVERNANCE PHASE CHAINING | NO PHASE-5 SCREEN REMEDIATION WITHOUT A NEW EXPLICIT ORDER; PHASE 4 IS IMPLEMENTED AND THE DETECTOR MAY BE RUN READ-ONLY, BUT NO FINDING MAY BE REMEDIATED | NO WAIVER, IGNORE-LIST OR SUPPRESSION MECHANISM IN THE CONFORMANCE DETECTOR WITHOUT A NEW EXPLICIT ORDER | NO SECOND SCREEN CONFORMANCE AND NO ARCHETYPE RATIFICATION FROM ONE CONFORMING SCREEN; NO ARCHETYPE IS RATIFICATION_ELIGIBLE | NO ISOLATED MICROPHASE CORRECTION OF KLEBER REVIEW DEFECTS; THEY MUST BE GROUPED INTO ONE BOUNDED STABILIZATION ORDER | NO REAL BUSINESS-FLOW RECREATION WITHOUT A NEW EXPLICIT ORDER | SUPERSEDED UNIT 5A DIRECTION IS CANCELLED

## Task-specific pointers

- `docs/ledgers/G28_LEDGER.md::## 2026-07-26 - UI-CONSOLIDATION-PHASE-5-PASS-3-CONTROL-HEIGHT-R1-A1 - fix(ui): close generic control height pass`
- `docs/architecture/ORDEM_COMPRA_LIFECYCLE_SPEC_PROPOSED.md::## §R.31 Active Phase-C continuation requirement registry — governance metadata`
- `docs/architecture/PEDIDO_OP_SCHEMA_CONTRACT.md::### 13.17 Active Phase-C schema requirement registry — governance metadata`
- `docs/architecture/PEDIDO_PRODUCTION_FLOW_BACKLOG.md::# Update 2026-07-22 - C5-DOCUMENTATION-CLOSEOUT-R1 (PHASE-C5 supervisor acceptance and closeout; OC-C5-EMISSION-001 SATISFIED)`
- `docs/governance/traceability/purchase-order-phase-c.json::/requirements`
